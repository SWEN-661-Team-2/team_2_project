// Extends Vitest's expect with jest-dom matchers (e.g. toBeInTheDocument, toHaveClass).
// Imported once in the Vitest setup file so matchers are available in all test files.
import '@testing-library/jest-dom/vitest';
